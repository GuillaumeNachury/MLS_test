/**
 * Common / transverse actions
 * @author: Guillaume Nachury 
 */
const reducer = (state, action) => {
    switch(action.type){
        default:
            return {...state}
    }

}

export default reducer;

